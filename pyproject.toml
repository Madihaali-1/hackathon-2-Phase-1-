[project]
name = "todo-app"
version = "0.1.0"
description = "A simple in-memory console-based todo application"
readme = "README.md"
requires-python = ">=3.13"
license = {text = "MIT"}
authors = [
    {name = "Developer", email = "dev@example.com"}
]